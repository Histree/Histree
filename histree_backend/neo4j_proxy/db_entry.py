
# Query, Driver, Parser
# what is the relation between query - parser? 1-1? n-n?
# each type (of flower) should have a parser for its petals


# we should know the driver type here.
def find_a_node(type: str | None, key: str, val) :
    # read
